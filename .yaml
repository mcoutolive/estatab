project:
  language: 'python'

build:
  python-version: '>=3.10'
  working-directory: 'src'
  architecture: 'x64'
  requirement-path: 'requirements.txt'

publish:
  exclude-folder-or-file: 'off'

test:
  working-directory: 'src'
  requirement-path: 'test-requirements.txt'

security:
  fortfy:
  enviroment-analyse: ''
